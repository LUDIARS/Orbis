import { describe, expect, it } from 'vitest'
import { actionIds, isActionId } from '../src/main/actions/registry.js'
describe('action registry', () => { it('only resolves registered action ids', () => { expect(actionIds).toHaveLength(19); expect(isActionId('indagatio.open')).toBe(true); expect(isActionId('habitus.cycle')).toBe(true); expect(isActionId('rota.open')).toBe(true); expect(isActionId('habitus.set:mobile')).toBe(true); expect(isActionId('comparatio.toggle')).toBe(true); expect(isActionId('unknown')).toBe(false); expect(isActionId('toString')).toBe(false); expect(isActionId('__proto__')).toBe(false) }) })
